require 'spec_helper'

describe LanguageDetection do
  it 'has a version number' do
    expect(LanguageDetection::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
